## PostgreSQL configuration (use this for local/dev with a running Postgres instance)
spring.datasource.url=jdbc:postgresql://localhost:5432/restaurante_db
spring.datasource.username=admin
spring.datasource.password=admin
spring.datasource.driver-class-name=org.postgresql.Driver

# Hibernate / JPA
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true

# Application context (optional)
# server.servlet.context-path=/api

spring.jpa.open-in-view=false
logging.level.org.springframework.security=DEBUG
logging.level.org.springframework.web=DEBUG

# --- JWT Configuration ---
# Chave secreta (SECRET) para o JWT (Base64, 256-bit para HS256)
# IMPORTANTE: Esta � uma chave segura. Mantenha-a em segredo.
jwt.secret=kO7qfT6L/n5bT5yqS3J4wG8yT4R2aV1bY5pX8cT3aE0=

# Tempo de expira��o do token (em milissegundos)
# 3600000 = 1 hora
# 86400000 = 24 horas
jwt.expiration=3600000